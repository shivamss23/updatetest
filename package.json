{
  "name": "ssapp",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build  --win --publish never",
    "deploy": "electron-builder build  --win --publish always",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^21.2.2",
    "electron-updater": "^5.3.0"
  },
  "devDependencies": {
    "electron-builder": "^23.6.0"
  },
  "repository": {
    "type" : "git",
    "url" : "https://github.com/shivamss23/updatetest.git"
  }
}
